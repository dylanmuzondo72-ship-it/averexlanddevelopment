// @ts-nocheck
import Link from "next/link"; import { PageHeader } from "@/components/dashboard/PageHeader"; import { requireDashboardUser } from "@/lib/dashboard/access";
export const dynamic="force-dynamic";
export default async function Units({searchParams}:{searchParams:Promise<{q?:string;availability?:string;verification?:string;propertyType?:string}>}){const p=await searchParams;const {supabase}=await requireDashboardUser();let q=supabase.from("land_units").select("id,internal_reference,stand_number,land_size,land_size_unit,property_type,availability_status,verification_status,land_developments(name,reference_number)").neq("availability_status","archived").order("created_at",{ascending:false}).limit(50);if(p.q)q=q.or(`internal_reference.ilike.%${p.q}%,stand_number.ilike.%${p.q}%,location_description.ilike.%${p.q}%`);if(p.availability)q=q.eq("availability_status",p.availability);if(p.verification)q=q.eq("verification_status",p.verification);if(p.propertyType)q=q.eq("property_type",p.propertyType);const {data}=await q;return <div className="dashboard-content"><PageHeader eyebrow="Land administration" title="Land units" actions={<Link className="dashboard-button dashboard-button-primary" href="/dashboard/land/units/new">Add land unit</Link>}/><form className="dashboard-filter-bar"><input name="q" placeholder="Search reference or stand" defaultValue={p.q}/><select name="availability" defaultValue={p.availability||""}><option value="">All availability</option><option>draft</option><option>available</option><option>reserved</option><option>sold</option><option>unavailable</option></select><select name="verification" defaultValue={p.verification||""}><option value="">All verification</option><option>unverified</option><option>under_review</option><option>verified</option></select><button className="dashboard-button dashboard-button-secondary">Filter</button></form><section className="dashboard-panel"><div className="dashboard-table-wrap"><table className="dashboard-table"><thead><tr><th>Reference</th><th>Development</th><th>Stand</th><th>Size</th><th>Availability</th><th>Verification</th></tr></thead><tbody>{(data||[]).map(u=><tr key={u.id}><td><Link href={`/dashboard/land/units/${u.id}`}>{u.internal_reference}</Link></td><td>{u.land_developments?.name||"-"}</td><td>{u.stand_number}</td><td>{u.land_size} {u.land_size_unit}</td><td>{u.availability_status}</td><td>{u.verification_status}</td></tr>)}</tbody></table></div>{(!data||data.length===0)&&<p className="dashboard-empty-copy">No land units found.</p>}</section></div>}
