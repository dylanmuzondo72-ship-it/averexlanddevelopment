// @ts-nocheck
import { notFound } from "next/navigation"; import { PageHeader } from "@/components/dashboard/PageHeader"; import { requireRoles } from "@/lib/dashboard/access"; import { updateDevelopmentAction } from "@/app/dashboard/land/actions";
export const dynamic="force-dynamic";
export default async function EditDevelopment({params,searchParams}:{params:Promise<{id:string}>;searchParams:Promise<{error?:string}>}){const {id}=await params;const p=await searchParams;const {supabase}=await requireRoles(["administrator","staff"]);const {data:d}=await supabase.from("land_developments").select("*").eq("id",id).maybeSingle();if(!d)notFound();return <div className="dashboard-content"><PageHeader eyebrow={d.reference_number} title="Edit development" description={p.error}/><form className="dashboard-form" action={updateDevelopmentAction.bind(null,id)}><label>Name<input name="name" required defaultValue={d.name}/></label><label>Slug<input name="slug" required defaultValue={d.slug}/></label><label>Development type<input name="developmentType" required defaultValue={d.development_type}/></label><label>Location<input name="location" required defaultValue={d.location}/></label><label>Status<select name="status" defaultValue={d.status}><option>draft</option><option>active</option><option>completed</option><option>archived</option></select></label><label>Address<input name="address" defaultValue={d.address||""}/></label><label>City/town<input name="cityTown" defaultValue={d.city_town||""}/></label><label>Province/region<input name="province" defaultValue={d.province||""}/></label><label>Total land size<input name="size" type="number" step="0.0001" defaultValue={d.total_land_size||""}/></label><label>Size unit<input name="sizeUnit" defaultValue={d.land_size_unit||""}/></label><label>Description<textarea name="description" defaultValue={d.description||""}/></label><label>Internal notes<textarea name="internalNotes" defaultValue={d.internal_notes||""}/></label><button className="dashboard-button dashboard-button-primary">Save development</button></form></div>}
