// @ts-nocheck
import Link from "next/link"; import { notFound } from "next/navigation"; import { PageHeader } from "@/components/dashboard/PageHeader"; import { requireDashboardUser } from "@/lib/dashboard/access";
export const dynamic="force-dynamic";
export default async function DevelopmentDetail({params}:{params:Promise<{id:string}>}){const {id}=await params;const {supabase}=await requireDashboardUser();const [{data:d},{data:units}]=await Promise.all([supabase.from("land_developments").select("*").eq("id",id).maybeSingle(),supabase.from("land_units").select("id,internal_reference,stand_number,property_type,land_size,land_size_unit,availability_status,verification_status").eq("development_id",id).neq("availability_status","archived").order("stand_number").limit(100)]);if(!d)notFound();return <div className="dashboard-content"><PageHeader eyebrow={d.reference_number} title={d.name} description={`${d.location} · ${d.status}`} actions={<Link className="dashboard-button dashboard-button-primary" href={`/dashboard/land/units/new?developmentId=${id}`}>Add land unit</Link>}/><section className="dashboard-panel"><p>{d.description||"No description."}</p><dl className="dashboard-details-grid"><div><dt>Development type</dt><dd>{d.development_type}</dd></div><div><dt>Land size</dt><dd>{d.total_land_size||"-"} {d.land_size_unit||""}</dd></div><div><dt>Address</dt><dd>{d.address||"-"}</dd></div><div><dt>Province</dt><dd>{d.province||"-"}</dd></div></dl></section><section className="dashboard-panel"><h2>Land units ({units?.length||0})</h2><div className="dashboard-table-wrap"><table className="dashboard-table"><thead><tr><th>Reference</th><th>Stand</th><th>Type</th><th>Size</th><th>Availability</th><th>Verification</th></tr></thead><tbody>{(units||[]).map(u=><tr key={u.id}><td><Link href={`/dashboard/land/units/${u.id}`}>{u.internal_reference}</Link></td><td>{u.stand_number}</td><td>{u.property_type}</td><td>{u.land_size} {u.land_size_unit}</td><td>{u.availability_status}</td><td>{u.verification_status}</td></tr>)}</tbody></table></div>{(!units||units.length===0)&&<p className="dashboard-empty-copy">No land units in this development.</p>}</section></div>}
